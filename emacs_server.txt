emacs(server)

emacs 서버 요약.

emacs gui 이외에 같이 딸려오는 server에 관한 설명.

일종의 로컬 서버이며 daemon과 유사함.

주 기능은, emacs를 서버처럼 상시 백그라운드에 올려놓는 것.
해당 기능으로 대용량, 고성능 패키지나 라이브러리를 미리 로드시켜서 대기시킬 수 있음.
emacs를 껐다 켤때마다의 로드 시간을 줄일 수 있음.
